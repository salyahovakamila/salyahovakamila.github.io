# Конфигурация публикации для MkDocs сайта
sites:
  - name: site1
    # Папка с готовым сайтом
    root: docs/
    # Ветка
    branch: main
    # Команда сборки (пустая, т.к. собираем локально)
    build_command: ""
    # Публикация включена
    publish:
      enabled: true
    # Индексный файл
    index: index.html